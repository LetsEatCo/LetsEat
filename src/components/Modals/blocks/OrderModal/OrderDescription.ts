import styled from 'styled-components';
import {Flex} from 'rebass';

export const OrderDescription = styled(Flex)`
	font-size: 14px;
	letter-spacing: 0.14px;
	font-weight: 400;
	line-height: 1.43;
	color: rgba(143, 149, 163, 0.9);
	margin-bottom: 5px;
	overflow: hidden;
`;
