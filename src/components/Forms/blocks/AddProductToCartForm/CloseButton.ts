import styled from 'styled-components';
import {CloseButton as CommonCloseButton} from '@/components/Common/CloseButton';

export const CloseButton: any = styled(CommonCloseButton)`
	position: relative;
	top: -24px;
	right: 0;
`;
